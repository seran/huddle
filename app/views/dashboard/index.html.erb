<div class="flex flex-col md:flex-row flex-grow">
  <aside class="w-full md:w-64 p-4">
    <ul>
      <li><%= link_to dashboard_view_path do %>Dashboard<% end %></li>
      <li><%= link_to team_view_path do %>Teams<% end %></li>
      <li><%= link_to reports_view_path do %>Reports<% end %></li>
    </ul>
    <ul>
      <li></li>
    </ul>
  </aside>

  <main class="flex-grow p-4 order-2 md:order-1">
    Boards
    <ul>
        <% @boards.each do |board| %>
          <li><%= link_to board_view_path(board.id) do %><%= board.name %><% end %></li>
        <% end %>
    </ul>
  </main>
</div>
